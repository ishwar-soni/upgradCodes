/*
package com.upGrad;

public class SLAPExample {
    public boolean verify(String userName){
        int userId = database.getUserIdFromUserName(userName);
        User user = userDirectory.getUserFromUserId(userId);
        return isUnique(user.username)
                && containsSpecialCharacters(user.password)
                && hasCorrectFormat(user.email)
                && isAdult(user.birthdate);
    }


        public boolean isValid(User user) {
            return isUnique(user.username)
                    && containsSpecialCharacters(user.password)
                    && hasCorrectFormat(user.email)
                    && isAdult(user.birthdate);
        }
}
*/
